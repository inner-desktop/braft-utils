{
  "name": "@inner-desktop/braft-utils",
  "version": "3.0.12",
  "description": "Utils for Braft Editor",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist/ && BABEL_ENV=production ./node_modules/.bin/babel src --copy-files --extensions .es6,.es,.js,.jsx --out-dir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "braft",
    "editor",
    "utils"
  ],
  "author": "Margox",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "@inner-desktop/braft-convert": "^2.3.0",
    "draft-js": "^0.10.5",
    "draftjs-utils": "^0.9.4",
    "immutable": "~3.7.4"
  }
}
